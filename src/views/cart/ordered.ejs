<div class="container">
    <div class="row ">
        <div class="">
            <div class="card">
                <div class="card-header">
                    <h5>Danh sách Sản phẩm Chờ duyệt và Giao hàng</h5>
                </div>
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">Sản phẩm</th>
                                <th scope="col">Số lượng</th>
                                <th scope="col">Giá sản phẩm</th>
                                <th scope="col">Trạng thái</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (var i = 0; i < ordereds.length; i++) { %>
                            <tr>
                                <td><%= ordereds[i].products[0].product_name %></td>
                                <td>1</td>
                                <td><%= ordereds[i].products[0].price %>$</td>
                                <td><%= ordereds[i].status %></td>
                            </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
                <div class="card-footer text-muted text-center">
                    <p>Vui lòng đợi trong khi sản phẩm của bạn được duyệt và giao hàng.</p>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .container{
  padding-top: 10px;
}
.card-header {
  background-color: #4CAF50; /* Màu xanh lá cây */
  color: white; /* Màu chữ trắng */
  text-align: center; /* Canh giữa */
  padding: 10px; /* Khoảng cách từ viền đến nội dung */
}

.table thead th {
  color: white; /* Màu chữ trắng */
  background-color: #4CAF50; /* Màu nền xanh lá cây */
  text-align: center; /* Canh giữa */
}

.table tbody tr {
  background-color: #f2f2f2; /* Màu nền xám nhạt */
}

</style>